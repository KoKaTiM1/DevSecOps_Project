## Phase X — Homepage polish

**Goal:** improve homepage content and UX.

### Updates:
- Update environment label from `local` to `aws` (or `server`) when running on AWS
- Add hover effect on homepage image (glow + slight popup)
- Add better project descriptions (clearer, more detailed)

### Details:
- Environment label should read `aws` (or `server`) when the app is deployed to EKS/EC2 and `local` during local dev
- Image hover effect should include a subtle glow, slight scale-up (e.g., 1.02–1.05), and smooth transition
- Descriptions should mention impact, tooling, and outcome (what the project does and why it matters)

